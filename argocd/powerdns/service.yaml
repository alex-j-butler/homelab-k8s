apiVersion: v1
kind: Service
metadata:
  name: powerdns-admin-static
  namespace: powerdns
  labels:
    app: powerdns-admin
spec:
  ports:
  - port: 9494
    protocol: TCP
  selector:
    app: powerdns-admin